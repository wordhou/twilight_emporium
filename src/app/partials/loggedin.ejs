<% if (user) { %>
  Logged in as <%= user.display_name %>
  <a href="/auth/logout" class="logout-button">Log out</a>
<% } else {%>
  <a href="/auth?redirect=<%= redirect %>" class="login-button"> Log in with Google
    <i class="bi bi-google"></i>
  </a>
<% }%>
